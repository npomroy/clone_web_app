<div class="home-callout text-center container-fluid">
    <div class="container">
        <div class="row">
            <div class="col-md-4 home-blurb">
              <h1 class="display-4">Welcome!</h1>
              <p class="lead">This is a simple collection of things I have recorded</p>
              <hr class="my-4">
              <p>Contact me for more info: npomroy@live.com</p>
            </div>
        </div>
    </div>
</div>

<div class="container">
    <div class="row">
        <div class="col-md-4">
            <div class="well">
                <h2>Outdoors</h2>
            </div>
        </div>
        <div class="col-md-4">
            <div class="well">
                <h2>Electronics</h2>
            </div>
        </div>
        <div class="col-md-4">
            <div class="well">
                <h2>Computing</h2>
            </div>
        </div>
    </div>
</div>
<div class="container">
    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            <div class="well">
                <% if user_signed_in? %>
                    <% if current_user.profile %>
                        <%= link_to "Edit Profile", edit_user_profile_path(user_id: current_user.id), class: "btn btn-default btn-lg btn-block" %>
                        <%= link_to "View Profile", user_path(id: current_user.id), class: "btn btn-default btn-lg btn-block" %>
                        <%= link_to "User Index", users_path, class: "btn btn-default btn-lg btn-block" %>
                    <% else %>
                        <%= link_to "Create Profile", new_user_profile_path(user_id: current_user.id), class: "btn btn-default btn-lg btn-block" %>
                    <% end %>
                <% else %>
                    <%= link_to "Sign In", new_user_session_path, class: "btn btn-default btn-lg btn-block" %>
                    <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-default btn-lg btn-block" %>
                <% end %>
      
                
            </div>
        </div>
    </div>
    
</div>
